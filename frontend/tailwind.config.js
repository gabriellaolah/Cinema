/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./frontend/**/*.html",                // Scan all HTML files in the root
    "./frontend/**/*.js",       // Scan all JavaScript files in the frontend folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
